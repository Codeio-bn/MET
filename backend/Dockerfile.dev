FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# src/ is mounted as a volume at runtime — no COPY needed
EXPOSE 3000

CMD ["npx", "nodemon", "--watch", "src", "src/index.js"]
